# Terraform Modules

> Planned — no modules implemented yet.

Terraform modules for deploying Devin integration infrastructure.

## Planned Modules

- `webhook-receiver` — Deploy a serverless webhook receiver (AWS Lambda, Azure Functions, or GCP Cloud Run)
- `api-gateway` — Managed API Gateway with auth and rate limiting in front of webhook endpoints
- `secrets` — Provision and rotate Devin API keys in your cloud provider's secrets manager
